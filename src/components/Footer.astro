---
import { Icon } from "astro-icon/components";
import { socials } from "@/constants";
import { Image } from "astro:assets";
import PyConLogo from "src/assets/common/pyconth_logo@1x.png";
---

<footer
  class="relative flex items-center justify-between overflow-clip bg-slate-100 pb-32 pt-4 sm:px-8 md:px-16 lg:px-16"
>
  <div class="flex-shrink p-4">
    <Image
      class="hidden md:flex"
      src={PyConLogo}
      alt="PyCon Thailand Logo"
      height={100}
    />
  </div>

  <div class="flex items-center max-sm:flex-col md:flex">
    <Image
      class="my-3 flex md:hidden"
      src={PyConLogo}
      alt="PyCon Thailand Logo"
      height={60}
    />
    <div class="px-4 text-center font-semibold text-gray-400">
      © 2025. PyCon Thailand. All rights reserved.
    </div>
    <div class="flex items-center gap-x-4 px-4 max-md:mt-4 md:flex">
      {
        socials
          .filter((social) => social.link !== "")
          .map((social, index) => (
            <a href={social.link} target="_blank">
              <Icon
                name={social.icon}
                color={social.color}
                width={24}
                height={24}
              />
            </a>
          ))
      }
    </div>
  </div>

  <div
    class="group absolute bottom-0 left-0 w-full translate-y-8 cursor-pointer text-center transition-all hover:translate-y-0 hover:opacity-100"
  >
    <span class="text-6xl opacity-25 group-hover:hidden">🐍</span>
    <a
      href="https://youtu.be/kMwodsPwrSQ?t=8"
      class="hidden text-6xl group-hover:block group-hover:animate-bounce"
    >
      🐘
    </a>
  </div>
</footer>
<style></style>
